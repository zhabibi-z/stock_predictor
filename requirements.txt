# Runtime dependencies — pinned for reproducibility
yfinance==0.2.54
scikit-learn==1.5.0
tensorflow==2.18.0
pandas==2.2.2
numpy==1.26.4
matplotlib==3.9.0
seaborn==0.13.2
streamlit==1.35.0
PyYAML==6.0.1
# scipy: already an indirect dependency of scikit-learn; used directly now
# for the normal distribution's CDF/PPF in the Deflated Sharpe Ratio (Tier 2).
scipy==1.13.1
# statsmodels: the Augmented Dickey-Fuller test for fractional-differentiation
# order selection (Tier 2) — the standard tool for this, not reimplemented by
# hand given how easy a stationarity test is to get subtly wrong.
statsmodels==0.14.2
